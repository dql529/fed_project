import numpy as np
from flask import Flask, request, jsonify
from Model import Net18
import torch
import torch.nn as nn
import pickle
import base64
import requests
import time
import multiprocessing
from time import sleep
from sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score

torch.manual_seed(0)
n_nodes = 8
node_train = []
node_test = []
for i in range(1, n_nodes + 1):
    node_train.append(torch.load(f"wifi_traffic_dataset/data_object/node_train_{i}.pt"))
    node_test.append(torch.load(f"wifi_traffic_dataset/data_object/node_test_{i}.pt"))

import torch

# Load category and color encodings
cat_dict = torch.load("wifi_traffic_dataset/data_object/server_train.pt")
print(cat_dict.num_node_features)
